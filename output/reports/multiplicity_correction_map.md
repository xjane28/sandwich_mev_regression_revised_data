# Multiple-Comparison Correction Map

This is an auto-generated reference table (see `src/m13_multiplicity_map.py`) mapping which correction family and clustering scheme applies to which test across the project's modules. It is regenerated directly from the same already-real, already-committed output tables the other modules produce -- family sizes and correction descriptions are read from those tables' own columns, not hardcoded. Nothing here changes any existing test, correction, or conclusion; it only documents, in one place, decisions already made independently inside each module (m8-m11).

| Module | Hypothesis | Test family | Family size | Correction | Clustering / inference unit | Decision-rule status |
|---|---|---|---|---|---|---|
| m8_h1_test.py | H1 (concentration) | Gini / top-1% share vs. operational benchmark, per window | 1 test per metric per window (24m primary; 30m is robustness re-run of the same test, not pooled into a family) | None -- single primary test per metric does not require a multiplicity correction | Nonparametric bootstrap (window-level) | Confirmatory (primary window only) |
| m9a_h2_test.py | H2 (conditional evidence) | Monthly conditional e-value/martingale tests (bins x directions x targets x windows) | 80 | Bonferroni, applied to the Markov-inequality p-bound on top of the fixed-stake supermartingale | Sequential, per-bin martingale (not cluster-based) | Descriptive/exploratory -- reported as "not identified", not a confirmatory decision |
| m9a_h2_test.py | H2 (matched uncertainty) | Hoeffding-type bound (bins x weighting x windows) | 40 | Family-adjusted Hoeffding radius (2*family/alpha term inside the bound itself) | Same as above | Descriptive interval, not a significance test |
| m9b_h2_test.py | H2 (grouped-binomial regression) | Small-vs-larger trade-size-bin contrasts, primary 24m specification | 10 | Holm FWER correction across 10 <$100-vs-larger contrasts within specification | CR1 project/version-clustered secondary contrast; two-sided t reference | Confirmatory (10/10 significant after Holm in the primary 24m specification) |
| m10_h3_test.py | H3 (primary temporal evolution) | Dencun/Pectra joint step+slope HAC(14) Wald tests (outcomes x upgrades) | 6 | Holm step-down | HAC(14), single time series (heteroskedasticity/autocorrelation-robust, no clustering) | Confirmatory (primary) |
| m10_h3_test.py | H3 (secondary formal tests) | Post-upgrade slope and omnibus temporal tests (outcomes x sub-tests) | 9 | Holm step-down (separate family from the primary joint-test family above) | HAC(14), same series | Secondary/formal, explicitly association not causal effect |
| m11_h4_test.py | H4 (pairwise protocol contrasts) | All pairwise log-odds contrasts between protocols with >=1 observed attack, 24m sample | 190 | Holm step-down, reported as p_value_holm_exploratory | CR1, clustered by calendar month (df=23) | exploratory diagnostic only: month-clustered CR1 with a limited number of clusters; no confirmatory significance classification |

## Why the families differ in size and structure

Each module's family is scoped to the tests it actually runs as a coherent group, not to the project as a whole. m9a runs the same conditional test independently across bins, directions, targets and windows, so its family is the full cross-product of those dimensions. m9b only corrects across the trade-size-bin contrasts within one specification -- it does not pool across specifications (24m primary vs. 30m robustness). m10 deliberately keeps two families apart (primary joint tests vs. secondary formal tests) because they answer different questions and mixing them would over- or under-correct one of the two. m11 applies Holm across all available pairwise contrasts for transparency, but labels the result exploratory rather than confirmatory, because with only ~24 independent monthly clusters, a family this large is past the point where the correction's own asymptotics are trustworthy -- the same reasoning that leads it to withhold the incidence omnibus and protocol-by-time tests entirely.

## Suggested paper placement

A short reference in a methods appendix (e.g. "Appendix: Multiple-Comparison Corrections"), cited once from the main text where multiplicity is first mentioned, so a reader can look up "which correction applied to which test" in one place rather than reconstructing it from five separate modules.
